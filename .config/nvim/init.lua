require("core")

-- local cwd = vim.uv.cwd() --cwd stands for current working directory
-- local file = vim.fs.find(function(name, _)
-- 	return name:match(".prettierrc")
-- end, { limit = 1, type = "file", path = cwd })
--
-- print(file[1]) -- return 1st string if there is one, if not returs nil
